<Window x:Class="ClipboardQueueApp.Views.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="{DynamicResource Bg}"
        Foreground="{DynamicResource Fg}"
        Title="Edit Item" Height="300" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Edit Content" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"
                   Foreground="{DynamicResource Fg}"/>

        <TextBox Grid.Row="1" x:Name="InputBox" 
                 AcceptsReturn="True" TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto"
                 Padding="5" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" Padding="15,5" Margin="0,0,10,0"/>
            <Button Content="Save" IsDefault="True" Click="Save_Click" Padding="15,5"/>
        </StackPanel>
    </Grid>
</Window>
